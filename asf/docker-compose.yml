services:
  asf:
    image: justarchi/archisteamfarm
    restart: unless-stopped
    container_name: asf
    volumes:
      - asf_data:/app/config
    ports:
      - 1242:1242

volumes:
  asf_data:
    external: true
