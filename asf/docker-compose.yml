services:
  asf:
    image: justarchi/archisteamfarm
    restart: unless-stopped
    container_name: asf
    volumes:
      - /srv/asf/config:/app/config
      - /srv/asf/logs:/app/logs
      - /srv/asf/plugins:/app/plugins
    ports:
      - 1242:1242
